package com.ruppyrup.javapoet.schema1;

import java.lang.Boolean;
import java.lang.Integer;
import java.lang.Number;
import java.lang.String;
import java.util.Arrays;
import java.util.List;
import lombok.Builder;
import lombok.Value;
import lombok.experimental.Accessors;

@Builder(
    setterPrefix = "with",
    builderClassName = "Builder",
    buildMethodName = "build0",
    toBuilder = true
)
@Accessors(
    fluent = true
)
@Value
public class Parent {
  @lombok.Builder.Default
  private String name = "Rupert";

  @lombok.Builder.Default
  private List<Boolean> coinToss = Arrays.asList(true,false,true);

  @lombok.Builder.Default
  private Boolean male = false;

  @lombok.Builder.Default
  private Integer age = 33;

  private Address address;

  @lombok.Builder.Default
  private List<String> hobbies = Arrays.asList("fishing","triathlon");

  @lombok.Builder.Default
  private List<Integer> luckyNumbers = Arrays.asList(77,39);

  @lombok.Builder.Default
  private Number yearsInHouse = 16.9;

  @lombok.Builder.Default
  private List<Integer> emptyInts = Arrays.asList();

  @lombok.Builder.Default
  private List<Number> meterReadings = Arrays.asList(16.9,120.9,200.64);

  public static class Builder {
    public Parent build() {
      return this.build0();
    }
  }
}
